// Reserved for future bespoke cookie storage. Right now we use
// PersistCookieJar with FileStorage directly in DioFactory.create() — this
// file exists so the structure is set up if/when we need to wrap it.
